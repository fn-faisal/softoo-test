import HomeScreen from "../pages/HomeScreen";

/**
 * Would be used to wrap the applicaiton router with stores etc here.
 * NOTE: since the app has only one screen, router was not used for this test.
 */
export default function App() {
    return (
        <HomeScreen />
    )
}
